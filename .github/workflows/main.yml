name: CI

on:
  push:
    branches: 
      - master

jobs:
  publish:
    name: deploy to github pages
    runs-on: ubuntu-latest
    
    steps:
      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
          
      - name: Deploy
        run: npm run deploy
    
